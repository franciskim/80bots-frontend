import React from 'react';
import AppLayout from '/components/default/layout';
import Profile from '/components/Profile';

const ProfilePage = () => <AppLayout title={'Profile'}> <Profile /> </AppLayout>;

export default ProfilePage;
