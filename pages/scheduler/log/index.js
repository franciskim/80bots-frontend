import React from 'react';
import SchedulerLog from '/components/SchedulerLog';
import AppLayout from '/components/default/layout';

const SchedulerLogPage = () => <AppLayout title={'Scheduler Log'} hideBanner> <SchedulerLog /> </AppLayout>;

export default SchedulerLogPage;
